/// A placeholder class that represents an entity or model.
class MeetingItem {
  const MeetingItem(this.id, this.title, this.participant);

  final int id;
  final String title;
  final List<String> participant;
}
